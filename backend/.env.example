# Directorios donde buscar archivos .log, separados por ':'
PSDASH_LOG_DIRS=/var/log:/logs
# Origenes permitidos para la SPA (CORS), separados por ','
PSDASH_CORS_ORIGINS=http://localhost:5173,http://localhost:8080
